{
  "name": "waxbanditgame",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "prettier": "^2.3.1",
    "reload": "^3.2.0",
  },
  "scripts": {
    "start": "reload --port 3000 --browser --start-page public/index.html --exts html,js",
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
